# A5-Family Exit And A7.2 Decision Preregistration

Date: 2026-06-27.

Status: accepted active A7.2 direction after Ben's 2026-06-28 instruction.
This document authorizes only the bounded work needed to turn A7.2 delayed
artifact-mediated endogenous prediction into a frozen preregistered
single-hive implementation/smoke gate. It does not authorize broad seed sweeps,
dashboards, external integrations, real LLM calls, Lean, Slack, browser
automation, Atomspace work, live task boards, immediate downstream multi-hive
coupling, or promotion language.

## Decision Context

A5, A5.1a, A6.2, and A7 long-horizon validation all closed conservatively on
their preregistered evidence standards. The latest GPT-5.5-Pro strategic review
marked the direction shift as `strategic_change_level: major` and
`notify_ben: true`, so OmegaSim paused for Ben's decision.

Ben has now resolved that governance pause: proceed with A7.2 first, then move
on to a three-hive ring whether A7.2 is positive or negative. Therefore:

- close A5-family predictive-control work as an evidence boundary;
- open A7.2 delayed artifact-mediated endogenous prediction as the next
  single-hive mechanism-rich gate;
- after A7.2 closes, write and execute a separate three-hive ring
  preregistration with cross-hive prediction costs, delayed artifact transfer,
  target/phase nulls, and transfer-opportunity controls.

## Evidence Boundary

| Gate | Scope | Positive result | Closure reason |
| --- | --- | --- | --- |
| A5 seed `7..16` | Single-hive anticipatory predictive-control confirmatory gate | Forecast skill and allocation alignment improved in some predictor conditions | Full residual accounting and matched null criteria did not support residual structure or strange-attractor-like claims |
| Reopened A5 seed `5,6` | Bounded single-hive smoke/pilot requested on 2026-06-27 | Linear/nonlinear predictors again improved forecast skill over reactive and shuffled controls | Full-accounting residual predictability stayed inside permutation/null criteria; no intermediate-budget condition passed all promotion gates |
| A5.1a seed `5,6` | Prediction spend charged against work opportunity with spend-only replay nulls | Charged predictors beat spend-only replay nulls on forecast skill | No charged cost rule beat its spend-only replay null on full-accounting residual predictability |
| A6.2 seed `1,2` | Long-horizon logistic appraisal residual-recurrence validation | Source schema and recurrence computations completed | Logistic did not beat linear and both source-preserving nulls on the same target with paired cross-seed agreement |
| A7 seed `1,2` | Semantic-field long-horizon residual/null validation | Source-accounted A7 artifact schema and analyzer path were exercised | Logistic semantic coupling did not establish residual/null superiority sufficient for semantic-dynamics promotion |

Interpretation boundary: these gates support only methodological claims about
deterministic artifacts, accounting controls, and null discipline. They do not
support lobe, attractor, synchrony, semantic-dynamics, or causal collective
structure claims.

## Active A7.2 Mechanism

A7.2 is a new preregistered single-hive gate rather than another A5 seed
broadening. The mechanism should make prediction endogenous and delayed:

- agents choose among `predict`, `work`, `review`, and `synthesize`;
- utilities are thresholded logistic functions of lagged forecast error,
  artifact readiness, contradiction, risk, fatigue, and adaptive thresholds;
- prediction consumes work opportunity and can update only lagged or delayed
  forecast/artifact state;
- review and synthesis depend on artifact readiness and contradiction/risk,
  not only queue depth;
- fatigue and adaptive thresholds create bounded hysteresis, with all slopes,
  delays, costs, and caps frozen before any run.

The scientific question would be whether delayed, artifact-mediated,
resource-bounded endogenous prediction produces residual structure that
survives accounting and null controls, not whether it merely improves forecast
skill or changes backlog.

## Required Endpoints

Primary endpoints should be frozen before implementation:

- forecast skill per prediction spend;
- residual predictability after accounting for clock/demand phase, task
  arrivals, service capacity, action opportunity, work budget, prediction
  spend, remaining work, backlog, queued age, completion fraction, starvation,
  and source-ledger components;
- lead-lag structure from lagged error/uncertainty to prediction spend and
  artifact state;
- delay-embedded recurrence or return structure in residual artifact/prediction
  state;
- macro-state compressibility or predictability that is not reducible to
  queue/accounting variables;
- productivity guardrails for completion fraction, backlog, queued age,
  starvation, prediction-spend volatility, and work-budget volatility.

## Required Nulls And Controls

A7.2 should not run without prospectively frozen controls:

- zero-budget reactive baseline;
- high-budget or oracle smoothing positive control;
- amplitude-matched linear control;
- same-tick logistic control;
- phase-shuffled lag-input control;
- threshold-shuffled control;
- source-preserving semantic label shuffle;
- spend-only replay null that deducts the same prediction/work opportunities
  while removing useful forecast timing.

Promotion would require an intermediate endogenous-prediction condition to beat
the relevant controls on preregistered residual endpoints while preserving
guardrails. Forecast skill alone is insufficient.

## Closure Rules And Accepted Roadmap

Close A7.2 fail-closed if residual endpoints are explained by accounting
controls, matched nulls, source-ledger components, backlog dwell, or work-loss
mediation. Do not rescue a negative result by tuning slopes, thresholds,
delays, costs, or caps after seeing residual plots.

After A7.2 closes, positive or negative, proceed to the three-hive ring as a
separate preregistration rather than implementing multi-hive coupling directly
from this document. That preregistration should carry forward the A7.2 lessons
about costly prediction, delay, thresholding, null discipline, and accounting
controls while adding target and phase nulls that only become meaningful with
three hives.
