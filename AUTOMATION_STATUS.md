# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The scheduler-ablation pilot-vs-holdout synthesis is complete. The demand-vs-service-capacity holdout on seeds `70..99` now supports the load-accounting interpretation while preserving a residual trajectory signal in point estimates: higher service capacity reduces load-normalized backlog at fixed pressure and increases lobe-transition entropy, while higher creation pressure increases load-normalized backlog at every fixed service level and reduces transition entropy. The paired bootstrap control supports the signs of nearly all trajectory/load effects, but the label-count-preserving null control makes the independent lobe-locking interpretation more conservative because observed-minus-null entropy/dwell differences are small. The decision synthesis now includes load/action accounting, confirming that task creation pressure changes created-task counts and action mix while service capacity changes task-work opportunity. The queue-blind lobe robustness pass, which excludes queue depth, queue delta, and default baseline lobe labels, still shows pressure structure: extreme-minus-normal pressure increases task-generation dwell share by roughly `0.47..0.49`, lowers transition entropy most strongly at low/baseline service, and high service capacity shifts dwell share toward execution. Treat pressure-induced trajectory locking as a candidate residual signal that survives an action-only robustness check, but still not yet as independent emergent lobe grammar because demand is implemented through create-task action weight.

## Latest Run

- Status: ok, 2026-06-25 added and ran the bounded exogenous-arrival calibration helper; no holdout grid was launched.
- Changed: added `python -m ohdyn.calibrate_exogenous_arrivals`, which runs endogenous/control and coupled-pressure target configs plus candidate exogenous rates over a supplied seed set, validates baseline attention shares and `quota_balance`, writes `exogenous_arrival_calibration.csv` and `summary.md`, and selects provisional rates using total created-task means only. Documented the helper in `README.md` and added regression coverage for its CSV schema, candidate run artifacts, and report sections. No real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, multi-hive coupling, new holdout grid, or duplicate A0/A1 mechanics were added.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: minor` and `notify_ben: false`. Its recommendation to perform calibration only after the RNG-stream and queue-blind label fixes was accepted; those prerequisites were already present, so this run proceeded to the bounded calibration helper. No recommendation was rejected or deferred.
- Source artifacts: bulky calibration run artifacts are under ignored `runs/a2_exogenous_arrival_calibration_20260625/`; compact tracked calibration artifacts are `docs/results/a2_exogenous_arrival_calibration.md` and `docs/results/a2_exogenous_arrival_calibration.csv`. Earlier bulky holdout artifacts remain under ignored `runs/a2_service_capacity_holdout_seed70_99_20260624/`, and queue-blind analysis artifacts remain under ignored `runs/a2_queue_blind_lobes_holdout_seed70_99_20260625/`.
- Result: seed `1..3` calibration with candidate rates `0.0, 0.5, 1.0, 1.5, 2.0, 2.5, 3.0` produced created-task targets `40.0` for endogenous control, `50.0` for the low exogenous midpoint, `60.0` for the high-pressure target, and `73.666667` for the extreme-pressure target. Provisional frozen exogenous rates are `1.0` for low (`49.333333` created mean), `2.0` for medium/high-pressure matching (`59.666667` created mean), and `3.0` for high/extreme-pressure matching (`74.666667` created mean). These are calibration fixtures only, not lobe-dynamics evidence.
- Verified: `git diff --check` passed; `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k 'exogenous_arrival_calibration or exogenous_arrivals or queue_blind_label_uses_agent_created'` passed with `5 passed, 532 deselected`; `.venv-conda/bin/python -m pytest tests/test_run_harness.py` passed with `537 passed`.
- Blockers: none.
- Next step: add frozen low/medium/high exogenous-arrival configs using rates `1.0`, `2.0`, and `3.0`, plus a bounded comparison helper scaffold for the primary holdout, without launching the holdout grid.
