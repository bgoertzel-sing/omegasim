# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The sixty-nine-seed value-yield divergence remains a same-direction degradation under both completion-normalized and effort-normalized yield, so the earlier sixty-five-seed opposite-sign tradeoff should be treated as an unstable transient hypothesis rather than a direction change. The scheduler-ablation pilot-vs-holdout synthesis is complete; the next mechanism-discriminating path is now preregistered as demand-vs-service-capacity using baseline attention shares and `quota_balance`, crossing creation pressure `1.0`, `1.8`, and `2.2` with work service capacity `0.7`, `1.0`, and `1.3`.

## Latest Run

- Status: ok, 2026-06-24 preregistered the compact demand-vs-service-capacity ablation requested by the previous next step.
- Changed: added deterministic `model.work_service_capacity` config loading with default `1.0`, applied it only to the baseline `work_task` action weight, added low/high service-capacity endpoint fixtures at pressure `1.0`, `1.8`, and `2.2`, and documented the preregistered grid in `README.md`. No real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, multi-hive coupling, or duplicate A0/A1 mechanics were added.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: minor` and `notify_ben: false`. Its recommendation to stop adding seed-only scheduler extensions and preregister a creation-pressure x service-capacity ablation was accepted as scientifically sensible. No GPT-5.5-Pro recommendation was rejected or deferred in this run.
- Source artifacts: no ignored large experiment artifacts were required; a focused smoke check used temporary output directories only.
- Result: the new service-capacity scalar preserves old configs by default and gives the next ablation a reproducible service axis. Under seed `23` at pressure `1.8`, low service capacity `0.7` worked `29` tasks and ended with queue depth `72`, baseline service capacity `1.0` worked `35` tasks and ended with queue depth `65`, and high service capacity `1.3` worked `52` tasks and ended with queue depth `38`.
- Verified: `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k 'service_capacity or pressure_fixtures or manifest_and_config_match_documented_a0_provenance_schema or config_only_writes_normalized_config or high_pressure_increases_creation_pressure or medium_pressure_sits_between'` passed with `21 passed, 501 deselected`; `.venv-conda/bin/python -m pytest tests/test_run_harness.py` passed with `522 passed`; `git diff --check` passed.
- Blockers: none.
- Next step: add a small deterministic demand-vs-service-capacity comparison helper that runs the preregistered 3x3 grid and emits load-normalized backlog, queued age, value per work event, capture pressure, and lobe transition/dwell summaries.
