# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The sixty-nine-seed value-yield divergence remains a same-direction degradation under both completion-normalized and effort-normalized yield, so the earlier sixty-five-seed opposite-sign tradeoff should be treated as an unstable transient hypothesis rather than a direction change. The next mechanism-discriminating path is now preregistered as a quota-balance versus seeded random-available scheduler ablation using the same baseline attention shares at pressure `1.0`, `1.8`, and `2.2`.

## Latest Run

- Status: ok, 2026-06-24 added the preregistered scheduler-mechanism ablation path for quota-balance versus seeded random-available task selection.
- Changed: added `attention_policy.selection_strategy` with default `quota_balance` and supported `random_available`; wired random-available selection to choose a queued task uniformly by the run's deterministic RNG seed while preserving capture-pressure telemetry for the actually selected class; added manifest and summary provenance for the selection strategy; added normal/high/extreme baseline-share random-available fixtures; added `docs/a2_scheduler_ablation_preregistration.md` with conditions, pressure points, primary outcomes, and interpretation rule; updated README and harness tests. No task policy shares, real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, multi-hive coupling, or A0/A1 baseline mechanics were added.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: major` and `notify_ben: true`; Ben should be notified. Its recommendation to freeze the 69-seed baseline and move to bootstrap/shuffled-prefix uncertainty analysis plus mechanism-discriminating perturbation or ablation experiments remains accepted as scientifically sensible. The former seed-extension loop remains deferred because ordered seed-prefix extension now has diminishing scientific return.
- Analysis run: none; this bounded run implemented and preregistered the ablation mechanism/config path rather than interpreting new experiment results.
- Result: the current quota-balancing scheduler remains the default for all existing attention configs, while `random_available` provides a deterministic same-share scheduler ablation at pressure `1.0`, `1.8`, and `2.2`. Focused tests confirm same-seed reproducibility and a distinct worked-task sequence versus quota balancing under the same seed.
- Verified: `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k 'attention_random_available or a2_attention_run_records_policy_metrics_and_summary or loads_a2_attention'` passed with `16 passed, 499 deselected`; `.venv-conda/bin/python -m pytest tests/test_run_harness.py` passed with `515 passed`; `git diff --check` passed.
- Blockers: none.
- Next step: run a small seed 1-3 quota-balance versus random-available scheduler ablation over pressure `1.0`, `1.8`, and `2.2`, then check in one compact result artifact summarizing the preregistered primary outcomes.
