# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are frozen as a negative/clarifying mechanism result. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The scheduler-ablation pilot-vs-holdout synthesis is complete. The demand-vs-service-capacity holdout on seeds `70..99` supports the load-accounting interpretation while preserving a residual trajectory signal in point estimates: higher service capacity reduces load-normalized backlog at fixed pressure and increases lobe-transition entropy, while higher creation pressure increases load-normalized backlog at every fixed service level and reduces transition entropy. The paired bootstrap control supports the signs of nearly all trajectory/load effects, but the label-count-preserving null control makes the independent lobe-locking interpretation more conservative because observed-minus-null entropy/dwell differences are small. The decision synthesis includes load/action accounting, confirming that task creation pressure changes created-task counts and action mix while service capacity changes task-work opportunity. The queue-blind lobe robustness pass, which excludes queue depth, queue delta, and default baseline lobe labels, still shows pressure structure under coupled pressure, but the primary frozen-rate exogenous-arrival holdout on seeds `70..99` is the decisive A2 demand-decoupling control: exogenous rate increases monotonically raise total created tasks, final load-normalized backlog, and queued age while agent-created task totals remain near the endogenous control. Paired bootstrap and label-count-preserving null controls support a conservative interpretation: high exogenous load-normalized backlog growth is sign-stable, queue-blind entropy decline is only moderately sign-stable, queue-blind task-generation dwell share does not grow, and baseline observed-minus-null dwell/entropy differences are small. Treat A2 as evidence for load pressure plus action-budget-mediated trajectory structure, with only a weak residual queue-blind entropy point estimate and no demonstrated independent emergent lobe grammar. A3 queue-flow/service synthesis on seeds `70..99` is now frozen in `docs/results/a3_queue_flow_service_synthesis_seed70_99.md`, with a concise closure gate in `docs/results/a3_closure_gate_seed70_99.md`: existing artifacts support load accounting, service-capacity response, and work-opportunity accounting; lagged service synchronization remains a modest exploratory diagnostic with analyzer-emitted paired seed uncertainty fields; no residual unexplained structure requires lobe grammar. The latest external strategic review is accepted as research approval for A4 smoke-contract implementation only, with `strategic_change_level: minor` and `notify_ben: false`; this is a narrow operational gate, not approval for A4 holdout runs. Current A4 work may implement only the opt-in YAML/config validation, inert two-hive smoke artifact path, deterministic coupling event schemas, append-safe outputs, and conservation/reproducibility tests described in `docs/a4_multihive_queue_flow_service_preregistration.md`. Do not add real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, three-hive mechanics, multi-hive holdouts, new lobe architectures, or broad A2/A3 residual-lobe sweeps unless a concrete artifact bug is found and separately justified.

## Latest Run

- Status: active, 2026-06-25 completed the next bounded A4 smoke-contract slice after rereading `README.md`, this status file, config inventory, harness tests, `docs/a4_multihive_queue_flow_service_preregistration.md`, and latest external strategic review. The review remains accepted as approval for A4 smoke implementation only; it does not authorize A4 holdout runs or a strategic pivot.
- Changed: added `configs/a4_two_hive_shuffled_smoke.yaml`; enabled `coupling.mode: shuffled` in the opt-in multi-hive coupled simulator path; added coupling-RNG target selection over eligible non-source hives; added a shuffled smoke test for same-seed byte reproducibility, deterministic coupling event schema, preserved source-hive opportunity/attempt marginals versus the paired direct smoke, and per-hive plus aggregate queue-flow conservation; and documented the shuffled smoke command plus its two-hive limitation in `README.md`. Configs without `hives` continue through the original single-hive path and artifact list.
- Strategic review: latest `../outputs/strategy-reviews/omegasim/latest-review.md` is marked `strategic_change_level: minor` and `notify_ben: false`. Accepted: implement deterministic shuffled coupling event schemas plus conservation/reproducibility tests as the next smoke-contract slice, preserve single-hive behavior when `hives` is absent, and do not run A4 holdouts. Deferred: A4 analysis scripts and scientific seed `100..129` holdouts. No recommendation was rejected, and no Ben notification is required by the current review header.
- Result: A0/A1 remains complete without duplicate implementation, and A4 now has reproducible inert none-mode, direct same-tick transfer, delayed fixed-lag transfer, and shuffled two-hive transfer smoke paths with deterministic event schemas and per-hive/aggregate conservation checks. Because the first shuffled smoke has only two hives, target assignment is structurally equivalent to the only non-source hive; treat it as a schema/conservation control, not as a phase-structure null. No real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integration, multi-hive holdout runs, three-hive mechanics, new lobe labels, duplicate A0/A1 baseline implementation, or new A2/A3 simulations were added.
- Verified: `.venv-conda/bin/pytest -q tests/test_run_harness.py -k 'a4_two_hive_shuffled or a4_two_hive_delayed or a4_two_hive_direct or a4_two_hive_none or invalid_a4 or loads_opt_in_two_hive or a0_cli_reproduces_artifacts_by_seed'` passed (`16 passed, 551 deselected`). `.venv-conda/bin/pytest -q tests/test_run_harness.py` passed (`567 passed in 342.48s`). `.venv-conda/bin/ruff check ohdyn/sim.py tests/test_run_harness.py` passed. Full `ruff check ohdyn tests` remains blocked by two unrelated pre-existing lint findings recorded in the previous run: unused local `first` in `ohdyn/analyze_queue_flow_service.py` and unused import `_format_regime_counts` in `ohdyn/compare_exogenous_arrivals.py`.
- Blockers: A4 scientific holdout runs remain blocked until A4 analysis scripts are stable and the smoke-contract artifacts are reviewed as sufficient for paired-seed comparisons. No current blocker for the next smoke-contract implementation slice.
- Next step: implement A4 smoke artifact analysis/preflight checks for the four two-hive modes without running scientific holdout seeds.
