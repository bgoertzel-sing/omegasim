# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The sixty-nine-seed value-yield divergence remains a same-direction degradation under both completion-normalized and effort-normalized yield, so the earlier sixty-five-seed opposite-sign tradeoff should be treated as an unstable transient hypothesis rather than a direction change.

## Latest Run

- Status: ok, 2026-06-24 added deterministic seed-level bootstrap rank-stability analysis for the frozen 69-seed A2 extreme-pressure baseline.
- Changed: extended `ohdyn.analyze_pressure` to emit `pressure_bootstrap_rank_stability.csv` with global pressure-response, class-specific capture-pressure, and value-yield divergence scopes; added CLI flags `--bootstrap-resamples` and `--bootstrap-seed`; updated pressure-analysis tests and README; added checked-in compact result artifacts `docs/results/a2_pressure_seed1_69_bootstrap_rank_stability.md` and `docs/results/a2_pressure_seed1_69_bootstrap_rank_stability_top.csv`. No configs, simulator mechanisms, A0/A1 scheduling, task policy shares, real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, or multi-hive coupling were changed.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: major` and `notify_ben: true`; Ben should be notified. Its recommendation to freeze the 69-seed baseline and move to bootstrap/shuffled-prefix uncertainty analysis plus mechanism-discriminating perturbation or ablation experiments remains accepted as scientifically sensible. The former seed-extension loop remains deferred because ordered seed-prefix extension now has diminishing scientific return.
- Analysis run: `.venv-conda/bin/python -m ohdyn.analyze_pressure --pressure-dir runs/a2_attention_extreme_pressure_compare_seed1_69_20260624T_run --out runs/a2_attention_extreme_pressure_analysis_seed1_69_20260624T_bootstrap3_20260624 --limit 10 --bootstrap-resamples 200 --bootstrap-seed 1` completed successfully.
- Result: the global top-response bootstrap strongly supports the frozen pressure baseline: internal-improvement final queue-depth normal-to-medium slope (`32.844204`) was selected in `193/200` resamples (`0.965`) with sign stability `1.0`; research-heavy and baseline queue-depth normal-to-medium slopes were selected only `0.02` and `0.015`. The class-specific capture-pressure result remains secondary: baseline peak long-term-research capture-pressure normal-to-medium slope (`-0.154400`) was selected in `80/200` resamples (`0.4`) with sign stability `1.0`. Value-yield divergence remains unstable: the full-seed winner is still research-heavy medium-to-high slope (`0.122085`), but bootstrap selected it only `15/200` times (`0.075`, sign stability `0.97`), while internal-improvement normal-to-medium value-yield divergence (`0.111792`) was bootstrap-favored (`128/200`, `0.64`, sign stability `1.0`).
- Verified: `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k pressure_analysis` passed with `42 passed, 469 deselected` after the code/test/doc updates; `.venv-conda/bin/python -m pytest tests/test_run_harness.py` passed with `511 passed`; `git diff --check` passed; the compact checked-in bootstrap CSV parsed with 9 rows; the 69-seed bootstrap analysis output was generated and its summary/CSV rows were inspected.
- Blockers: none.
- Next step: add a small preregistered scheduler-mechanism ablation config path and comparison summary that contrasts the current quota-balancing task selection against a simple random available-task selector under the frozen pressure comparison setup.
