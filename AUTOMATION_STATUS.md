# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The sixty-nine-seed value-yield divergence remains a same-direction degradation under both completion-normalized and effort-normalized yield, so the earlier sixty-five-seed opposite-sign tradeoff should be treated as an unstable transient hypothesis rather than a direction change. The scheduler-ablation pilot-vs-holdout synthesis is complete; the mechanism-discriminating path is now demand-vs-service-capacity using baseline attention shares and `quota_balance`, crossing creation pressure `1.0`, `1.8`, and `2.2` with work service capacity `0.7`, `1.0`, and `1.3`. A deterministic helper now runs that 3x3 grid and emits load-normalized backlog, queued age, value per work event, capture pressure, and baseline lobe transition/dwell summaries.

## Latest Run

- Status: ok, 2026-06-24 added the compact deterministic demand-vs-service-capacity comparison helper requested by the previous next step.
- Changed: added `ohdyn.compare_service_capacity`, which runs the preregistered 3x3 creation-pressure x service-capacity grid over a deterministic seed set, validates increasing pressure/capacity axes, validates baseline attention shares and `quota_balance`, writes per-cell run artifacts, writes `service_capacity_comparison_metrics.csv`, and writes a top-level `summary.md`. The aggregate reports load-normalized backlog, queued age, value per work event, capture pressure, and baseline lobe transition/dwell summaries. Documented the command and output contract in `README.md` and added focused tests for grid coverage, schema/header stability, and reproducibility. No real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, multi-hive coupling, or duplicate A0/A1 mechanics were added.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: minor` and `notify_ben: false`. Its recommendation to stop adding seed-only scheduler extensions and move to creation-pressure x service-capacity analysis was accepted as scientifically sensible. No GPT-5.5-Pro recommendation was rejected or deferred in this run.
- Source artifacts: no ignored large experiment artifacts were required; direct CLI smoke used a temporary output directory only.
- Result: `python -m ohdyn.compare_service_capacity --seeds 1 --out <tmpdir>` produced nine pressure/service run directories plus `service_capacity_comparison_metrics.csv` and `summary.md`, giving the preregistered service-capacity ablation a reproducible aggregate artifact.
- Verified: `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k 'service_capacity_comparison or service_capacity_changes or service_capacity_fixtures'` passed with `10 passed, 515 deselected`; `.venv-conda/bin/ruff check ohdyn/compare_service_capacity.py tests/test_run_harness.py` passed; `.venv-conda/bin/python -m pytest tests/test_run_harness.py` passed with `525 passed`; `.venv-conda/bin/python -m ohdyn.compare_service_capacity --seeds 1 --out <tmpdir>` passed; `git diff --check` passed.
- Blockers: none.
- Next step: run the demand-vs-service-capacity comparison on a holdout seed block and interpret whether load-normalized backlog, queued age, value per work event, capture pressure, or baseline lobe dwell/transition summaries show residual dynamics beyond raw load accounting.
