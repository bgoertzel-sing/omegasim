# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The sixty-nine-seed value-yield divergence remains a same-direction degradation under both completion-normalized and effort-normalized yield, so the earlier sixty-five-seed opposite-sign tradeoff should be treated as an unstable transient hypothesis rather than a direction change. The next mechanism-discriminating path is now preregistered as a quota-balance versus seeded random-available scheduler ablation using the same baseline attention shares at pressure `1.0`, `1.8`, and `2.2`.

## Latest Run

- Status: ok, 2026-06-24 added the compact pilot-vs-holdout scheduler-ablation synthesis requested by the previous next step.
- Changed: added `docs/results/a2_scheduler_ablation_synthesis.md` and `docs/results/a2_scheduler_ablation_synthesis.csv`, normalizing the generic `research_heavy` policy label to `random_available` and comparing seed `1..3` against holdout seeds `70..99`. No simulator behavior, real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, multi-hive coupling, or A0/A1 baseline mechanics were added.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: minor` and `notify_ben: false`. Its recommendation to make the pilot-vs-holdout synthesis analytic with paired scheduler-effect and load-normalized backlog estimates was accepted as scientifically sensible. No GPT-5.5-Pro recommendation was rejected or deferred in this run.
- Source artifacts: reused existing ignored artifacts under `runs/a2_scheduler_ablation_seed1_3_20260624/`, `runs/a2_scheduler_ablation_seed1_3_analysis_bootstrap_20260624/`, `runs/a2_scheduler_ablation_holdout_seed70_99_20260624/`, and `runs/a2_scheduler_ablation_holdout_seed70_99_analysis_bootstrap_20260624/`.
- Result: the holdout confirms that pressure-amplified backlog survives the scheduler swap. Mean final queue depth rises from `21.5 -> 48.2 -> 57.733333` under `quota_balance` and `21.6 -> 45.5 -> 56.166667` under `random_available`. Paired holdout `random_available - quota_balance` queue-depth effects are small relative to the pressure effect: normal `0.1`, medium `-2.7`, high `-1.566667`, and high-minus-normal `-1.666667`. Load-normalized normal-to-high queue growth is nearly identical: `1.209774` queue-depth units per added created task for `quota_balance` and `1.200961` for `random_available`. This keeps raw queue depth as a robust load observable, not yet an emergent lobe-dynamics claim. The most relevant lobe-dynamics bridge is the holdout dwell signal: `random_available - quota_balance` high-minus-normal longest dwell is `1.2` with bootstrap interval `[0.166667, 2.466667]`.
- Verified: generated synthesis values from existing `comparison_metrics.csv` and per-run final `metrics.csv` rows; `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k 'analyze_pressure or pressure_bootstrap or value_yield_divergence or compare_pressure'` passed with `6 passed, 509 deselected`; `git diff --check` passed.
- Blockers: none.
- Next step: preregister a compact demand-vs-service-capacity ablation using baseline attention shares and `quota_balance`, with primary outcomes load-normalized backlog, queued age, value per work event, capture pressure, and lobe transition/dwell summaries.
