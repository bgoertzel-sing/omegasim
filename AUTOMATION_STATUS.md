# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are pivoting from ordered seed-prefix extension to mechanism-discriminating analysis. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The scheduler-ablation pilot-vs-holdout synthesis is complete. The demand-vs-service-capacity holdout on seeds `70..99` now supports the load-accounting interpretation while preserving a residual trajectory signal in point estimates: higher service capacity reduces load-normalized backlog at fixed pressure and increases lobe-transition entropy, while higher creation pressure increases load-normalized backlog at every fixed service level and reduces transition entropy. The paired bootstrap control supports the signs of nearly all trajectory/load effects, but the label-count-preserving null control makes the independent lobe-locking interpretation more conservative because observed-minus-null entropy/dwell differences are small. Treat pressure-induced trajectory locking as a candidate residual signal, not yet as independent emergent lobe grammar.

## Latest Run

- Status: ok, 2026-06-25 added deterministic paired bootstrap uncertainty and label-count-preserving null controls over the existing service-capacity holdout trajectory artifacts.
- Changed: extended `ohdyn.analyze_service_capacity_trajectory` to emit `service_capacity_trajectory_bootstrap.csv` and `service_capacity_trajectory_nulls.csv` in addition to the previous metrics/effects/summary artifacts. Added tests for the new headers, reproducibility, and summary sections. Tracked compact holdout outputs under `docs/results/a2_service_capacity_trajectory_controls_seed70_99.md`, `docs/results/a2_service_capacity_trajectory_controls_seed70_99_bootstrap.csv`, `docs/results/a2_service_capacity_trajectory_controls_seed70_99_nulls.csv`, and `docs/results/a2_service_capacity_trajectory_controls_seed70_99_effects.csv`. No real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, multi-hive coupling, new experiments, or duplicate A0/A1 mechanics were added.
- Strategic review: `../outputs/strategy-reviews/omegasim/latest-review.md` exists and is marked `strategic_change_level: minor` and `notify_ben: false`. Its recommendation to add paired seed-bootstrap intervals and label-count null controls before launching another experiment was accepted as scientifically sensible. The load/action accounting panel and queue-blind lobe labeling were deferred, not rejected, because they are the next analysis layers after the uncertainty/null-control pass.
- Source artifacts: bulky per-run holdout artifacts remain under ignored `runs/a2_service_capacity_holdout_seed70_99_20260624/`; the new control analysis artifacts remain under ignored `runs/a2_service_capacity_trajectory_holdout_seed70_99_controls_20260625/`; compact tracked result artifacts are under `docs/results/`.
- Result: paired bootstrap over seeds `70..99` supports the primary signs: 29 of 30 bootstrap rows have sign stability `>=0.95`; all highlighted transition-entropy and max-dwell contrasts have sign stability `1.0`. Service-capacity increases still reduce load-normalized backlog and increase transition entropy at fixed pressure; creation-pressure increases still increase load-normalized backlog, reduce transition entropy, lengthen max dwell, and increase `backlog_growth` dwell share at fixed service capacity. The label-count-preserving null control is conservative for independent lobe-locking claims: observed-minus-null entropy and dwell-length differences are small, with strongest dwell excess only `0.203` ticks at `extreme_pressure / baseline_service`.
- Verified: `.venv-conda/bin/python -m pytest tests/test_run_harness.py -k 'service_capacity_trajectory or service_capacity_comparison_runner_writes_grid_summary'` passed with `4 passed, 524 deselected`; `.venv-conda/bin/ruff check ohdyn/analyze_service_capacity_trajectory.py tests/test_run_harness.py` passed; `.venv-conda/bin/python -m ohdyn.analyze_service_capacity_trajectory --service-capacity-dir runs/a2_service_capacity_holdout_seed70_99_20260624 --out runs/a2_service_capacity_trajectory_holdout_seed70_99_controls_20260625 --bootstrap-reps 1000 --null-reps 500 --random-seed 271828` completed and wrote 9 metric rows, 6 effect rows, 30 bootstrap rows, and 9 null-control rows.
- Blockers: none.
- Next step: add a load/action accounting panel to the service-capacity decision synthesis from the existing holdout artifacts before running another experiment.
