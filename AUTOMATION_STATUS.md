# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

The A5 anticipatory predictive-control loop is complete against the fresh seed `7..16` confirmatory evidence. Ben requested that stage on 2026-06-26 after the A4 closure, with the additional hypothesis that prediction effort itself is a scarce managed resource: zero prediction is reactive, perfect prediction may smooth away interesting dynamics, and intermediate resource-bounded prediction may create structured forecast errors whose resulting collective dynamics become cheaply predictable high-level patterns. The checked-in preregistration is `docs/a5_anticipatory_predictive_control_preregistration.md`, the prospective confirmatory rules are in `docs/a5_confirmatory_addendum.md`, and the closure note is `docs/results/a5_closure_note_seed7_16.md`. Treat the result as forecast-skill improvement without promotion-worthy residual structured dynamics. Preserve the A5 result as background rather than reopening A5 mechanics.

Ben supplied a new post-A5 direction on 2026-06-27 and then accepted the A6/A7/A8 sequence as the provisional OmegaSim experimentation roadmap. The roadmap is captured in `docs/omegasim_provisional_experiment_roadmap.md`, with the detailed A6 design in `docs/a6_logistic_appraisal_attractor_preregistration.md`. Treat A6 as the next scientific target centered on latent motivational state, semantic/artifact state, sigmoid/softmax action selection over appraisal signals, costly endogenous prediction, hysteresis, adaptive thresholds, thresholded artifact handoff, and residual latent-state attractor analysis. The immediate next OmegaSim task is an explicit A6 implementation gate: freeze state schemas, action utility equations, artifact fields, seed/noise streams, smoke configs, output schemas, deterministic artifact tests, and read-only analysis skeletons before any broad simulation sweep.

Phase A2 pressure/attention experiments are frozen as a negative/clarifying mechanism result. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The scheduler-ablation pilot-vs-holdout synthesis is complete. The demand-vs-service-capacity holdout on seeds `70..99` supports the load-accounting interpretation while preserving a residual trajectory signal in point estimates: higher service capacity reduces load-normalized backlog at fixed pressure and increases lobe-transition entropy, while higher creation pressure increases load-normalized backlog at every fixed service level and reduces transition entropy. The paired bootstrap control supports the signs of nearly all trajectory/load effects, but the label-count-preserving null control makes the independent lobe-locking interpretation more conservative because observed-minus-null entropy/dwell differences are small. The decision synthesis includes load/action accounting, confirming that task creation pressure changes created-task counts and action mix while service capacity changes task-work opportunity. The queue-blind lobe robustness pass, which excludes queue depth, queue delta, and default baseline lobe labels, still shows pressure structure under coupled pressure, but the primary frozen-rate exogenous-arrival holdout on seeds `70..99` is the decisive A2 demand-decoupling control: exogenous rate increases monotonically raise total created tasks, final load-normalized backlog, and queued age while agent-created task totals remain near the endogenous control. Paired bootstrap and label-count-preserving null controls support a conservative interpretation: high exogenous load-normalized backlog growth is sign-stable, queue-blind entropy decline is only moderately sign-stable, queue-blind task-generation dwell share does not grow, and baseline observed-minus-null dwell/entropy differences are small. Treat A2 as evidence for load pressure plus action-budget-mediated trajectory structure, with only a weak residual queue-blind entropy point estimate and no demonstrated independent emergent lobe grammar. A3 queue-flow/service synthesis on seeds `70..99` is now frozen in `docs/results/a3_queue_flow_service_synthesis_seed70_99.md`, with a concise closure gate in `docs/results/a3_closure_gate_seed70_99.md`: existing artifacts support load accounting, service-capacity response, and work-opportunity accounting; lagged service synchronization remains a modest exploratory diagnostic with analyzer-emitted paired seed uncertainty fields; no residual unexplained structure requires lobe grammar. A prior external strategic review approved the existing A4 two-hive seed `100..129` holdout bundle and read-only analyzer, with `strategic_change_level: minor` and `notify_ben: false`; no Ben notification was required and no strategic pivot was made. The preregistered A4 holdout has now been run locally under ignored `runs/` artifacts, summarized in `docs/results/a4_two_hive_holdout_seed100_129.md`, and closed conservatively in `docs/results/a4_decision_note_seed100_129.md`. The preregistered A4 delayed-coupling temporal null analysis found that delayed-minus-none completion-fraction correlations at lag 0 and lag 2 survived the circular-shift null, while load-backlog correlations, queued-age divergence, and completion-fraction divergence did not. The follow-up A4 accounting-control analyzer is now complete in `docs/results/a4_accounting_control_seed100_129.md`: raw and clock-only completion-fraction synchrony still survive, transfer-timing alone does not explain the signal, but opportunity/load controls and the combined non-tautological accounting controls move both lag endpoints inside the residualized circular-shift null. The A4 closure note in `docs/results/a4_closure_note_seed100_129.md` freezes the seed `100..129` interpretation as queue-flow/service and action-opportunity accounting plus a documented delayed synchronization diagnostic, not independent lobe grammar or justification for three-hive mechanics. Do not add real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, three-hive mechanics, new lobe architectures, or broad A2/A3 residual-lobe sweeps as part of the A6 gate. A7 semantic-field and A8 three-hive ring work are downstream stages after the A6 smoke scaffold and controls are stable.

- Status: A6 analysis-gate control-delta pass completed, 2026-06-27. This implements the current `AUTOMATION_STATUS.md` next step and the external GPT-5.5-Pro review recommendation to strengthen the read-only analyzer before any broader simulation or promotion claim. No strategic pivot was made; the review header remains `strategic_change_level: none` and `notify_ben: false`.
- Changed: `ohdyn.analyze_a6_logistic_appraisal` now emits richer per-run endpoint accounting and a new `a6_logistic_appraisal_control_deltas.csv` with paired seed-level logistic-minus-linear, logistic-minus-phase-shuffled, and logistic-minus-threshold-shuffled deltas. The delta rows include latent, artifact, handoff, prediction-spend, task, queue, service/work, message, and action-opportunity fields plus explicit `missing_required_fields`. Manifest statuses now distinguish paired-delta preflight from `residualization_not_yet_computed` and `promotion_not_evaluated`. Added focused pytest coverage for the new control-delta artifact. No simulator mechanics, configs, broad simulations, real LLM calls, dashboards, Lean, Slack, browser automation, Atomspace integrations, downstream multi-hive coupling, or new lobe architectures were added.
- Result: Running the updated analyzer over the existing seed `1..2` A6 smoke comparison wrote 6 paired control-delta rows with 44 columns and no missing required fields under ignored `runs/a6_logistic_appraisal_analysis_control_deltas_v2/`. The paired rows confirm action opportunity is matched in these smoke artifacts. Seed-level logistic-versus-linear artifact-utility deltas split sign (`+0.032` for seed 1, `-0.028586` for seed 2), so A6 remains smoke/analyzer evidence only; residual recurrence and clock/queue residualization are still not evaluated. A5 remains frozen as forecast-skill improvement without promotion-worthy residual structured dynamics.
- Verification: `.venv-conda/bin/python -m py_compile ohdyn/analyze_a6_logistic_appraisal.py` passed; `.venv-conda/bin/python -m pytest tests/test_run_harness.py -q -k 'a6 and analysis'` passed with `2 passed, 593 deselected`; `.venv-conda/bin/python -m ohdyn.analyze_a6_logistic_appraisal --compare-dir runs/a6_logistic_appraisal_compare --out runs/a6_logistic_appraisal_analysis_control_deltas_v2` passed; `.venv-conda/bin/python -m pytest tests/test_run_harness.py -q -k 'a6 or automation_guard'` passed with `16 passed, 579 deselected`; `.venv-conda/bin/python -m ohdyn.automation_guard` returned `state: open`, `should_noop: false`, `strategic_change_level: none`, and `notify_ben: false`.
- Blockers: none.
- Recommended next step: extend the read-only A6 analyzer with a per-tick residual preflight over the existing smoke artifacts, residualizing artifact/latent endpoints against tick, queue depth, queue delta, created/completed/worked counts, action counts, and prediction budget; report residual variance/autocorrelation only, without broadening seeds or making promotion claims.
