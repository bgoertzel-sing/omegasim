# OmegaSim Automation Status

This file is maintained by the OmegaSim research automation so progress can be checked from GitHub, including from a phone.

## Current Focus

Phase A2 pressure/attention experiments are frozen as a negative/clarifying mechanism result. A0/A1 baseline infrastructure was rechecked and already covers deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, summary.md, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries; no duplicate A0/A1 implementation should be added. Freeze the 69-seed extreme-pressure result as the current A2 pressure baseline: pressure-amplified queue depth is the robust baseline-level finding, the internal-improvement normal-to-medium queue-depth response is ordered-prefix and bootstrap stable, class-specific capture-pressure responses are secondary because bootstrap top-selection probability is modest, and value-yield divergence is secondary because ordered-prefix stability fails and bootstrap favors a different row from the full-seed winner. The scheduler-ablation pilot-vs-holdout synthesis is complete. The demand-vs-service-capacity holdout on seeds `70..99` supports the load-accounting interpretation while preserving a residual trajectory signal in point estimates: higher service capacity reduces load-normalized backlog at fixed pressure and increases lobe-transition entropy, while higher creation pressure increases load-normalized backlog at every fixed service level and reduces transition entropy. The paired bootstrap control supports the signs of nearly all trajectory/load effects, but the label-count-preserving null control makes the independent lobe-locking interpretation more conservative because observed-minus-null entropy/dwell differences are small. The decision synthesis includes load/action accounting, confirming that task creation pressure changes created-task counts and action mix while service capacity changes task-work opportunity. The queue-blind lobe robustness pass, which excludes queue depth, queue delta, and default baseline lobe labels, still shows pressure structure under coupled pressure, but the primary frozen-rate exogenous-arrival holdout on seeds `70..99` is the decisive A2 demand-decoupling control: exogenous rate increases monotonically raise total created tasks, final load-normalized backlog, and queued age while agent-created task totals remain near the endogenous control. Paired bootstrap and label-count-preserving null controls support a conservative interpretation: high exogenous load-normalized backlog growth is sign-stable, queue-blind entropy decline is only moderately sign-stable, queue-blind task-generation dwell share does not grow, and baseline observed-minus-null dwell/entropy differences are small. Treat A2 as evidence for load pressure plus action-budget-mediated trajectory structure, with only a weak residual queue-blind entropy point estimate and no demonstrated independent emergent lobe grammar. A3 queue-flow/service synthesis on seeds `70..99` is now frozen in `docs/results/a3_queue_flow_service_synthesis_seed70_99.md`, with a concise closure gate in `docs/results/a3_closure_gate_seed70_99.md`: existing artifacts support load accounting, service-capacity response, and work-opportunity accounting; lagged service synchronization remains a modest exploratory diagnostic with analyzer-emitted paired seed uncertainty fields; no residual unexplained structure requires lobe grammar. The next-stage multi-hive queue-flow/service study is preregistered as a design artifact only in `docs/a4_multihive_queue_flow_service_preregistration.md`; do not add multi-hive simulator mechanics until that preregistration is reviewed and converted into a concrete implementation plan. Automation policy: if the worktree is clean, the A3/A4 closure gates remain satisfied, no concrete artifact bug is found, and no human-approved preregistered implementation objective exists, stop without a new status-only commit. Stop broad A2 residual-lobe mechanism sweeps unless a concrete artifact bug is found.

## Latest Run

- Status: blocked, 2026-06-25 completed a bounded non-interactive A4 preregistration revision against the current source of truth. Read the README, status file, config inventory, harness tests, `docs/research_transition_directive.md`, `docs/results/a3_closure_gate_seed70_99.md`, `docs/a4_multihive_queue_flow_service_preregistration.md`, and the latest external strategic review before choosing the next step. No concrete artifact bug, failing baseline contract, or scientifically sensible reason to reopen A2/A3 sweeps was found. The only repository-approved forward motion was to revise the A4 preregistration/interface gate, not to implement simulator mechanics.
- Changed: revised `docs/a4_multihive_queue_flow_service_preregistration.md` to incorporate the latest GPT-5.5-Pro recommendations: explicit primary estimands, fixed primary contrasts, no best-lag primary endpoint, delay/shuffle semantics, transfer-volume caveats, independent RNG stream provenance, paired-seed stream reuse across coupling modes, and per-hive plus aggregate queue-flow conservation tests. No simulator mechanics, configs, dashboards, LLM calls, Lean, Slack, browser automation, Atomspace integration, multi-hive coupling implementation, new lobe labels, duplicate A0/A1 baseline implementation, new A2/A3 simulations, or A4 holdout runs were added.
- Strategic review: latest `../outputs/strategy-reviews/omegasim/latest-review.md` is marked `strategic_change_level: minor` and `notify_ben: false`. Accepted: make A4 causal estimands, null controls, RNG streams, lag handling, and flow-conservation identities explicit before implementation; stop closure-only A2/A3 commits; keep the existing A3 closure gate in force; preserve queue-flow/service accounting as the frozen primary interpretation; keep lagged synchronization descriptive only; and run no new A2/A3 simulations unless a concrete artifact bug is found. Deferred: actual multi-hive simulator mechanics remain blocked until human/research approval of the revised A4 YAML and artifact interface. No recommendation was rejected, and no Ben notification is required by the current review header.
- Result: A0/A1 remains complete without duplicate implementation: deterministic YAML-loaded runs, 15 static OmegaHive-like agents, one task queue, one NetworkX bus graph, metrics/events output, `summary.md`, reproducibility tests, role/action metrics, lobe labels, and lobe-transition/dwell summaries are already covered. A2 residual lobe grammar remains closed under the current single-hive simulator and label scheme. A3 queue-flow/service accounting remains frozen as the primary interpretation for future preregistered work. The A4 preregistration is now sharper but still a design artifact only; automation must not silently self-approve it.
- Verified: `.venv-conda/bin/pytest -q tests/test_run_harness.py -k 'loads_a0 or metrics_csv_records_role_action_counts or metrics_csv_records_baseline_lobe_labels or metrics_csv_records_baseline_lobe_transitions or summary_records_baseline_lobe_dwell_runs or readme_a0_smoke_first_milestone_command_preserves_full_schema_provenance or readme_a0_smoke_same_seed_reproduces_full_first_milestone_artifacts'` passed (`15 passed, 537 deselected`), and `.venv-conda/bin/python -m ohdyn.run --config configs/a0_smoke.yaml --seed 1 --out "$tmpdir"` exited `0` and wrote `config.yaml`, `manifest.yaml`, `metrics.csv`, `events.csv`, and `summary.md`.
- Blockers: A4 simulator code is blocked on human/research approval of the revised YAML and artifact schemas in `docs/a4_multihive_queue_flow_service_preregistration.md`.
- Next step: approve the revised A4 YAML and artifact interface in `docs/a4_multihive_queue_flow_service_preregistration.md` before implementing any multi-hive simulator mechanics.
